import bcrypt from 'bcryptjs';import jwt from 'jsonwebtoken';import User from '../models/User.js';
const safe=u=>({id:u._id,name:u.name,email:u.email,profilePicture:u.profilePicture,bio:u.bio,role:u.role,status:u.status,createdAt:u.createdAt});const token=u=>jwt.sign({id:u._id.toString(),role:u.role,status:u.status},process.env.JWT_SECRET,{expiresIn:process.env.JWT_EXPIRES_IN||'1d'});
export async function register(req,res,next){try{const {name,email,password,profilePicture='',bio='',role='student'}=req.body;if(!name||!email||!password)return res.status(400).json({success:false,message:'Name, email and password are required'});if(!['student','clubLeader'].includes(role))return res.status(400).json({success:false,message:'Invalid registration role'});const hashed=await bcrypt.hash(password,12);const u=await User.create({name,email,password:hashed,profilePicture,bio,role,status:role==='clubLeader'?'pending':undefined});res.status(201).json({success:true,user:safe(u),token:token(u)});}catch(e){next(e)}}
export async function login(req,res,next){try{const {email,password}=req.body;if(!email||!password)return res.status(400).json({success:false,message:'Email and password are required'});const u=await User.findOne({email}).select('+password');if(!u||!(await bcrypt.compare(password,u.password)))return res.status(401).json({success:false,message:'Invalid credentials'});res.json({success:true,user:safe(u),token:token(u)});}catch(e){next(e)}}
export async function me(req,res,next){try{const u=await User.findById(req.user.id);res.json({success:true,user:safe(u)});}catch(e){next(e)}}
