import {useState} from 'react';import {useNavigate,Link} from 'react-router-dom';import {useAuth} from '../context/AuthContext.jsx';export default function Register(){const [f,setF]=useState({name:'',email:'',password:'',role:'student'}),[error,setError]=useState('');const {register}=useAuth(),nav=useNavigate();return <section className="auth"><h1>Create account</h1>{error&&<div className="error">{error}</div>}<form onSubmit={async e=>{e.preventDefault();try{await register(f);nav('/')}catch(x){setError(x.response?.data?.message||'Registration failed')}}}><input required placeholder="Full name" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input required type="email" placeholder="Email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input required minLength="6" type="password" placeholder="Password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><select value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="student">Student</option><option value="clubLeader">Club Leader</option></select><button>Register</button></form><p>Already registered? <Link to="/login">Login</Link></p></section>}
