import{useEffect,useState}from'react';import{Link}from'react-router-dom';import api from'../services/api.js';export default function Events(){const[events,setEvents]=useState([]),[q,setQ]=useState(''),[category,setCategory]=useState(''),[type,setType]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=()=>{setLoading(true);setError('');const params={};if(q)params.q=q;if(category)params.category=category;if(type)params.type=type;api.get('/events',{params}).then(r=>setEvents(r.data.events)).catch(x=>setError(x.response?.data?.message||'Could not load events')).finally(()=>setLoading(false))};useEffect(load,[]);return <><section className="hero"><span className="eyebrow">GIU CAMPUS COMMUNITY</span><h1>Find your next campus experience.</h1><p>Discover workshops, socials, competitions and volunteering opportunities.</p></section><section className="toolbar"><input placeholder="Search events" value={q} onChange={e=>setQ(e.target.value)}/><select value={category} onChange={e=>setCategory(e.target.value)}><option value="">All categories</option>{['Academic','Sports','Arts','Technology','Social','Career','Volunteering','Other'].map(x=><option key={x}>{x}</option>)}</select><select value={type} onChange={e=>setType(e.target.value)}><option value="">All types</option>{['workshop','social','competition','volunteering','other'].map(x=><option key={x}>{x}</option>)}</select><button onClick={load}>Search</button></section>{error&&<div className="error">{error}</div>}{loading?<p>Loading events…</p>:!events.length?<p>No events match your search.</p>:<section className="grid">{events.map(e=><article className="card" key={e._id}><span className="badge">{e.category}</span><h2>{e.title}</h2><p className="muted">{e.club} · {new Date(e.eventDate).toLocaleString()}</p><p>{e.description}</p><p><b>Capacity:</b> {e.totalSlots} · <b>Status:</b> {e.status}</p><Link className="button secondary" to={'/events/'+e._id}>View details</Link></article>)}</section>}</>}
