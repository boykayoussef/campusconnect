import {createContext,useContext,useEffect,useState} from 'react';import api from '../services/api.js';const C=createContext();export function AuthProvider({children}){const [user,setUser]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{if(!localStorage.getItem('cc_token'))return setLoading(false);api.get('/auth/me').then(r=>setUser(r.data.user)).catch(()=>localStorage.removeItem('cc_token')).finally(()=>setLoading(false))},[]);const login=async(email,password)=>{const r=await api.post('/auth/login',{email,password});localStorage.setItem('cc_token',r.data.token);setUser(r.data.user)};const register=async body=>{const r=await api.post('/auth/register',body);localStorage.setItem('cc_token',r.data.token);setUser(r.data.user)};const logout=()=>{localStorage.removeItem('cc_token');setUser(null)};return <C.Provider value={{user,loading,login,register,logout}}>{children}</C.Provider>}export const useAuth=()=>useContext(C);
