import{useEffect,useState}from'react';import{Link}from'react-router-dom';import api from'../services/api.js';export default function MyEvents(){const[e,setE]=useState([]);useEffect(()=>{api.get('/events/mine').then(r=>setE(r.data.events))},[]);return <section><h1>My events</h1><div className="grid">{e.map(x=><article className="card" key={x._id}><span className="badge">{x.category}</span><h2>{x.title}</h2><p>{new Date(x.eventDate).toLocaleString()}</p><Link className="button secondary" to={'/events/'+x._id}>View</Link><Link className="button secondary" to={'/events/'+x._id+'/registrants'}>Registrants</Link></article>)}</div></section>}
