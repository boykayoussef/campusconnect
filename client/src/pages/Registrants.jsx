import{useEffect,useState}from'react';import{useParams}from'react-router-dom';import api from'../services/api.js';export default function Registrants(){const{id}=useParams(),[r,setR]=useState([]);useEffect(()=>{api.get('/registrations/event/'+id).then(x=>setR(x.data.registrations))},[id]);return <section><h1>Registrants</h1><div className="list">{r.map(x=><article className="row" key={x._id}><div><b>{x.user.name}</b><p>{x.user.email}</p></div><span className="badge">{x.status}</span></article>)}</div></section>}
