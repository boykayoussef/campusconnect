import {useState} from 'react';import {useNavigate,Link} from 'react-router-dom';import {useAuth} from '../context/AuthContext.jsx';export default function Login(){const [f,setF]=useState({email:'',password:''}),[error,setError]=useState('');const {login}=useAuth(),nav=useNavigate();return <section className="auth"><h1>Login</h1>{error&&<div className="error">{error}</div>}<form onSubmit={async e=>{e.preventDefault();try{await login(f.email,f.password);nav('/')}catch(x){setError(x.response?.data?.message||'Login failed')}}}><input required type="email" placeholder="Email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input required type="password" placeholder="Password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><button>Login</button></form><p>No account? <Link to="/register">Register</Link></p></section>}
