import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import api from '../services/api.js';export default function Events(){const [events,setEvents]=useState([]),[q,setQ]=useState('');const load=()=>api.get('/events',{params:q?{q}:undefined}).then(r=>setEvents(r.data.events));useEffect(load,[]);return <><section className="hero"><span className="eyebrow">GIU CAMPUS COMMUNITY</span><h1>Find your next campus experience.</h1><p>Discover workshops, socials, competitions and volunteering opportunities.</p></section><section className="toolbar"><input placeholder="Search events" value={q} onChange={e=>setQ(e.target.value)}/><button onClick={load}>Search</button></section><section className="grid">{events.map(e=><article className="card" key={e._id}><span className="badge">{e.category}</span><h2>{e.title}</h2><p className="muted">{e.club} · {new Date(e.eventDate).toLocaleString()}</p><p>{e.description}</p><Link className="button secondary" to={'/events/'+e._id}>View details</Link></article>)}</section></>}
