import {useEffect,useState} from 'react';import api from '../services/api.js';export default function Profile(){const [f,setF]=useState({name:'',bio:'',profilePicture:''}),[msg,setMsg]=useState('');useEffect(()=>{api.get('/users/profile').then(r=>setF({name:r.data.user.name,bio:r.data.user.bio||'',profilePicture:r.data.user.profilePicture||''}))},[]);return <section className="panel"><h1>My profile</h1><form onSubmit={async e=>{e.preventDefault();await api.put('/users/profile',f);setMsg('Profile saved')}}><label>Name<input value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Bio<textarea value={f.bio} onChange={e=>setF({...f,bio:e.target.value})}/></label><label>Profile picture URL<input value={f.profilePicture} onChange={e=>setF({...f,profilePicture:e.target.value})}/></label><button>Save</button></form>{msg&&<p className="success">{msg}</p>}</section>}
