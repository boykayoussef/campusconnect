import{useState}from'react';import{useNavigate}from'react-router-dom';import api from'../services/api.js';export default function EventForm(){const nav=useNavigate(),[f,setF]=useState({title:'',club:'',description:'',location:'',eventDate:'',type:'workshop',totalSlots:20});const save=async e=>{e.preventDefault();try{await api.post('/events',{...f,totalSlots:Number(f.totalSlots)});nav('/my-events')}catch(x){alert(x.response?.data?.message||'Could not create event')}};return <section className="panel"><h1>Create event</h1><form onSubmit={save}>{['title','club','location'].map(k=><input key={k} required placeholder={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<textarea required placeholder="Description" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/><input required type="datetime-local" value={f.eventDate} onChange={e=>setF({...f,eventDate:e.target.value})}/><select value={f.type} onChange={e=>setF({...f,type:e.target.value})}>{['workshop','social','competition','volunteering','other'].map(x=><option key={x}>{x}</option>)}</select><input min="1" type="number" value={f.totalSlots} onChange={e=>setF({...f,totalSlots:e.target.value})}/><button>Create event</button></form></section>}
